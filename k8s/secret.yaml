apiVersion: v1
kind: Secret
metadata:
  name: movie-secrets
  namespace: movie-app
type: Opaque
stringData:
  # WARNING: This is INSECURE for production!
  # Generate a secure SECRET_KEY using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
  SECRET_KEY: "INSECURE-CHANGE-IN-PRODUCTION-USE-RANDOM-50-CHAR-KEY"
  DB_PASSWORD: "moviepass"
